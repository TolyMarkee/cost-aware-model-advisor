# Raw case inputs — select one per isolated response

This file contains inputs, not expected answers. E01–E12 preserve the original user prompts. E13–E16 cover revision 3 boundaries. Conductor: supply only the selected prompt to the responding agent.

## E01

用三句话解释什么是递归。

## E02

从一份很长的会议记录中提取日期、负责人和待办事项，不评价内容。请推荐模型。

## E03

将仓库中 100 个文件里的旧包名机械替换为新包名，并运行现有测试。请推荐模型。

## E04

判断这一条生产数据库删除语句是否安全。请推荐模型。

## E05

定位一个跨 20 个文件、只在高并发下出现的数据竞争并提交修复。请推荐模型。

## E06

帮我做一个最厉害的 AI 平台。

## E07

我只能使用 Luna、Terra 和 Sol。请为普通单文件 Python 修改选择模型。

## E08

请使用 Model-X，但当前界面没有 Model-X。

## E09

只允许最低成本模型且禁止升级，帮我评估生产权限系统。

## E10

我现在只整理需求，后续要实现多 Agent、持久化和故障恢复。分别推荐当前阶段和实施阶段配置。

## E11

这是项目背景和 API Key：API_KEY_PLACEHOLDER。生成模型建议和可复用上下文卡。

## E12

在没有基准结果的情况下，精确判断 Terra High 和 Sol Medium 哪个成功率高多少。

## E13

上一轮建议我用“均衡档／medium”整理需求；我已照做，结果能用。现在任务、预算、可用模型都没变，也没有失败，继续整理下一节。仅回答是否需要重新选模型，不要展开项目方案。

## E14

下面是我提供的假设配置表，不是现实产品资料：A 费用1单位，只读文本，不支持工具；B 费用2单位，支持仓库文件、shell和low/medium；C 费用8单位，支持同样工具和high。只允许从这三项选。我要在仓库里机械替换包名并运行现成测试，没有复杂重构。请推荐配置，不实际执行。

## E15

这是一个假设的路由场景，不实际访问网络：我已固定用均衡档／medium，预算禁止升级。拉取需求文档连续两次报网络超时，文档还没拿到。模型还没开始分析。现在是不是应该换更强的模型？给我下一步建议。

## E16

旧上下文卡写着“只做桌面版，不做网页”，这是上个月的决定。现在我明确改为先做网页版，桌面版暂缓。请更新最短的交接卡，标清当前决定；不要假装文件已保存或下个对话已读取。

